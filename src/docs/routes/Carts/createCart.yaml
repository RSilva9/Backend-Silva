paths:
  /api/carts/:
    post:
      summary: Crea un carrito
      tags:
        - Carts
      responses:
        "200":
          description: "Crea un nuevo carrito"
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#components/schemas/Cart'